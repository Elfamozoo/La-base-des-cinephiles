fetch(search_url + new URLSearchParams({
            api_key: api_key,
            language: langue,
            page: 1,
            include_adult: true,
        });


        form.addEventListener('submit', (e) => {})